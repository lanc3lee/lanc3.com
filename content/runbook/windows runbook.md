


if port 445 is open
smbclient -L //<TARGET-IP> -N

if port 139 is open
usually means the machine is running an older version of SMB or is configured for legacy Windows compatibility

enum4linux-ng -A <TARGET-IP>

